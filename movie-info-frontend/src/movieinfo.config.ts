export const config = { 
  "useMockHttpCalls" : false  // should always be set to false in checked-in version; applies to all frontend HTTP calls, whether in the app, in React Testing Library, or in Playwright
};